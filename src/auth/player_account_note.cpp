#include "auth/auth_service.hpp"
#include <map>
namespace tsto {
// Player-facing account operations are intentionally exposed through AuthService so the router can enforce the bearer session first.
}
